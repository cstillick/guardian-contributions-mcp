# Dependencies for the VERCEL serverless deploy only (read API + web UI).
# Local dev and the ingestion worker use pyproject.toml via `uv sync` — this file
# is the lean subset the serverless function needs at import time. pypdf / openpyxl
# / mcp / apscheduler / uvicorn are intentionally excluded to keep the function
# small; ingestion runs off-Vercel (see .github/workflows/ingest.yml).
fastapi>=0.110
sqlalchemy>=2.0
jinja2>=3.1
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
psycopg[binary]>=3.1
